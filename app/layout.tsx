import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mariya Benny — AI Engineer & Full-Stack Developer",
  description:
    "AI Engineer and Full-Stack Developer. Led a 13-member tech team at a LegalTech startup from zero to 300+ active users. MSc AI & Data Science, CUSAT (top 1% of cohort). France Excellence Charpak Scholar.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
